# 判题Docker配置
judge.docker.image=judge-env
judge.docker.memory-limit=512m
judge.docker.cpu-limit=1.0
judge.docker.network=none
judge.docker.timeout=10000

# 编译器路径配置
judge.compiler.java=javac
judge.compiler.cpp=g++
judge.compiler.python=python

# 判题限制
judge.time-limit=5000
judge.memory-limit=256